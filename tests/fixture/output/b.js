



const {
    [(FOO            ).a]: a,
    [(FOO            ).b]: b,
    [(FOO            ).c]: c,
} = {}       ;

const {
    data: {
        d,
        e,
        f,
    } = {}            ,
}           = FOO || {};

(function({
    name,
    regex,
    uuidType = String            ,
}          ) {
});

let g          ,
    h           ,
    i          ;
