{
    "name": "ts-blank-space-website",
    "private": true,
    "type": "module",
    "imports": {
        "#r": "ts-blank-space/register"
    },
    "scripts": {
        "build": "node -C LKG --import=#r ./build.ts",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d ./dist"
    },
    "dependencies": {
        "monaco-editor": "^0.50.0",
        "ts-blank-space": "..",
        "typescript": "^5.5.4"
    },
    "devDependencies": {
        "esbuild": "^0.23.0",
        "gh-pages": "^6.1.1"
    }
}
