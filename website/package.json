{
  "name": "ts-blank-space-website",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d ./dist"
  },
  "dependencies": {
    "monaco-editor": "^0.50.0",
    "ts-blank-space": ".."
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "gh-pages": "^6.1.1"
  }
}
